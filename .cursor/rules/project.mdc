---
description: EdgeVerify project rules — apply to all files
globs:
alwaysApply: true
---

# EdgeVerify Project Rules

## What This Project Is

Serverless web app on **Cloudflare Pages + Pages Functions** that displays a visitor's connection info (IP, ISP, VPN/proxy, geolocation) in real time. No database. No logging. No user data persistence.

## Directory Layout

```
EdgeVerify/
├── src/
│   ├── main.tsx / App.tsx
│   ├── index.css               # Tailwind CSS v4 + shadcn/ui vars (orange theme)
│   ├── types/api.ts
│   ├── lib/utils.ts
│   ├── hooks/useConnectionInfo.ts
│   └── components/
│       ├── ui/                 # shadcn/ui components
│       ├── HeroCard.tsx
│       ├── InfoCard.tsx
│       ├── BrowserCard.tsx
│       └── CopyButton.tsx
├── functions/api/info.js       # Edge function → GET /api/info
├── dist/                       # Build output (gitignored)
└── index.html                  # Vite entry HTML
```

## Coding Rules

### Backend (`functions/`)
- ES Modules only: `export async function onRequest(context)`
- Runs on the **Cloudflare Workers runtime** (not Node.js — some Node APIs are unavailable)
- `context.request.cf` contains Cloudflare metadata (empty `{}` in local dev)
- Always set fallback values; never let an unhandled error break the response

### Frontend (`src/`)
- **React 19 + TypeScript** — function components only, no class components
- **shadcn/ui** components in `src/components/ui/` — do not modify shadcn internals
- **Tailwind CSS v4** via `@tailwindcss/vite` plugin — no `tailwind.config.ts` needed
- Do not introduce new npm dependencies without discussion (package manager: pnpm)

### Design
- White background (`--background: oklch(1 0 0)`)
- Orange primary accent (`--primary: oklch(0.705 0.213 47.604)` ≈ orange-500)
- Destructive (red) for Tor detection; amber/orange for VPN warning

## Key Constraints

| Item | Constraint |
|------|-----------|
| ip-api.com | HTTP only (free tier), 45 req/min rate limit |
| `cf.isTor` | Non-public Cloudflare property — may be absent |
| `cf.botManagement.score` | Paid Cloudflare plan only |
| Local dev `request.cf` | Always `{}` — all cf fields fall back to defaults |
| Node.js | v21 — use Vite v5 (Vite v8 requires Node 22+) |

## Security Non-Negotiables

- Backend must never persist user data (no DB writes, no logging to external services)
- Every response: `Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate`
- `Access-Control-Allow-Origin: *` is intentional (public read-only API)

## Reference

- `docs/architecture.md` — system design and data source table
- `docs/api-spec.md` — full JSON response schema
- `docs/vpn-detection.md` — VPN detection flow and edge cases
